name: Deploy Next App

on:
    # Auto-deploy on push disabled during testing; run manually from the Actions tab.
    # push:
    #     branches: ["main"]
    #     paths:
    #         - src/**
    workflow_dispatch:

permissions:
    contents: read
    id-token: write

jobs:
    build-and-deploy:
        uses: ./.github/workflows/deploy-app.yml
        with:
            package-path: "src"
            app-name: "sanskagarwal"
            deploy-type: "webapp"
            node-version: "24.x"
        secrets: inherit
